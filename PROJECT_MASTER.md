# 威尼斯饺子 QQ点名机器人 - 项目开发主控文档

> **文档版本**：v1.6.0  
> **最后更新**：2026-09-20 20:20  
> **文档定位**：本项目的“单一事实来源（Single Source of Truth）”。所有开发活动必须严格遵循 `Project_init_prompt.md` 中的技术规范。

---

## 一、 项目需求与核心定位

### 1. 业务需求
- **核心功能**：QQ群成员通过 `@机器人` 发送特定指令（如“查未点名”），机器人自动查询点名系统JSON接口，并将“未点名人员名单”回复至当前QQ群。
- **交互场景**：仅限QQ普通群聊环境，支持并发查询，响应时间需控制在 3 秒内。

### 2. 技术架构约束
- **运行载体**：国内主流 Serverless 云函数（推荐腾讯云 SCF）。
- **通信协议**：**强制**使用 QQ 开放平台官方 Bot API（`GROUP_AT_MESSAGE_CREATE` 事件）。
- **数据源**：HTTP 请求调用点名系统 JSON 接口。
- **安全红线**：所有敏感配置**必须**通过环境变量注入，严禁硬编码。

---

## 二、 项目开发步骤分解 (Milestones)

### 阶段 1：项目初始化与环境准备 ✅ [已完成]
- [x] 确立项目技术架构与核心需求。
- [x] 制定 AI 协作强制规范（`Project_init_prompt.md`）。
- [x] 定义标准化项目目录结构与基础代码骨架。
- [x] **用户已完成**：创建 QQ 机器人并成功拉入目标群聊。
- [x] **用户已确认**：数据源为 JSON 格式传输。

### 阶段 2：核心业务逻辑开发 ✅ [已完成]
- [x] 获取点名系统 JSON 接口真实结构（南大表格外部应用，鉴权/字段/业务规则实测确认，见 `docs/rollcall_api.md`）。
- [x] 实现 `src/api.py`：拉取点名表、按 metadata 动态解析列、汇总生成「未报/异常/已报」回复。
- [x] 实现 QQ 官方消息发送模块（`src/qq_sender.py`：access token 获取/缓存 + 群聊被动回复）。
- [x] 在 `src/bot.py` 中集成事件入口、回调地址验证（Ed25519，op=13）与指令路由。
- [x] 补充核心业务单元测试（Ed25519 RFC 8032 官方向量、指令路由、token 与消息发送请求格式、api 解析与汇总，17 项全绿）。
- [x] 真实接口端到端冒烟测试通过（本机直连南大表格，返回结果与表格实际数据一致）。

### 阶段 3：本地联调与模拟测试 ✅ [已完成]
- [x] 构造 QQ 平台事件样例（op=13 回调验证、op=0 群消息、base64 请求体）本地全链路验证 `main_handler`（`local_e2e.py`，回复内容与真实表格数据一致）。
- [x] 性能验证：全链路（含真实点名查询）单次 0.04~0.10 秒，远低于 3 秒要求；各 HTTP 调用 5 秒超时兜底。
- [x] 真实凭据验证：QQ access token 获取成功；伪造群号调发送接口返回「群不存在」（code 11255）而非鉴权错误，证明鉴权与请求格式均正确。

### 阶段 4：云端部署与平台配置 🔄 [进行中]
- [x] 腾讯云 SCF 创建事件函数 `rollcall-bot`（广州 / Python 3.9 / 执行方法 `bot.main_handler` / 128MB / 超时 10 秒 / 公网访问启用），代码包 `deploy/rollcall_bot.zip` 上传成功。
- [x] 云函数环境变量注入：`QQ_BOT_APP_ID`、`QQ_BOT_APP_SECRET`、`ROLLCALL_API_TOKEN`（实测云端取值正确、生效）。
- [x] 创建**函数 URL** 触发器（API 网关触发器对新用户已下线，不可用）：公网访问启用、授权类型「开放」、参数兼容模式启用（保持 API 网关请求/响应格式，`bot.py` 的集成响应结构无需改动）。
- [x] 云端出网验证：SCF 可访问 `table.nju.edu.cn`（点名数据拉取成功、无 `rollcall api error`）与 `api.bot.qq.com`（access token 获取成功，实测 `expires_in=881`）；全链路含真实查询耗时 **691ms**，满足 3 秒要求。
- [x] QQ 开放平台回调地址已保存为函数 URL，**op=13 Ed25519 签名校验通过**，接入方式已由 WebSocket 切换为 Webhook。
- [x] 上传修复后的代码包（`send_group_reply` 异常已捕获，避免云函数 430 崩溃导致平台重推）。部署时间 2026-09-20 08:41:13；云端复验：伪造群号请求返回 `HTTP 200 {"send_failed": "HTTP Error 400: Bad Request"}`（修复前为 430 崩溃），点名数据拉取与 token 获取均正常，单次 690ms。
- [x] QQ 平台「接收事件配置」已设为**全部接收**并保存成功（刷新后保持，接口返回 `retcode:0`，查询回读 `all_subscription:true`）。**根因记录**：此前保存反复静默回退为「部分接收 0/51」，接口实际返回 `40001 签名校验不通过` / `40002 回调地址状态同步失败`——平台在保存设置时会重新向回调地址发 op=13 验签，而云端签名耗时 2.9~3.1 秒、正好撞上 3 秒超时。解法：`src/ed25519.py` 改为扩展坐标无需求逆加法 + 预计算 `2^i·B` 表 + 公钥按种子缓存，签名 2.9 秒 → **0.2 秒**，RFC 8032 向量与 18 项单测全部通过。
- [x] 真实群端到端复验 **已通过（13:28 用户在本班群发送 `@威尼斯饺子 查未点名`，机器人正常回复名单）**。阶段 4 至此全部验收。

### 阶段 4.5：「@ 未报宿舍长」绑定功能（路线 B）🔄 [10 位已登记生效，仅剩真实群终验]
- [x] @ 能力实测：三轮真实群测试定案——`<qqbot-at-user id="<member_openid>" />` **只在 markdown（`msg_type:2`）里渲染成蓝色 @**，且**能 @ 非触发者**（详见 `docs/qq_bot_api.md` §6）。
- [x] 存储方案定案（零新增云产品、不动班级共享表格）：绑定结果由班长收集 → 以云函数环境变量 **`QQ_ROSTER_JSON`**（`{学号: member_openid}`）注入；身份核对用「绑定消息里的学号 ↔ SeaTable 宿舍长学号列」。
- [x] 代码落地：新增指令 **`绑定 <学号>`**（核对成功回复「学号=openid」供班长登记；学号不存在/未带参数各有提示）；「查未点名」改为**两条被动回复**——`msg_seq=1` 文本名单（格式不变）+ `msg_seq=2` markdown @ 清单（已绑定的真 @，未绑定的列宿舍号与姓名）；`api.py` 抽出 `summarize()` / `find_leader()`；临时探针指令 `测试at` 已删除。
- [x] 测试：**31 项单测全绿**（新增 @ 清单、roster 解析容错、绑定三分支、markdown 载荷）；`local_e2e.py` 增加「绑定」两用例，真实表格数据下四类回复均正确。
- [x] 部署：代码包 8062 字节经控制台「本地上传zip包」上线，函数 URL 复验返回 `{"replied":"rollcall","pending":14,"sent":[false,false]}`（假群号故发送失败，`pending=14` 与本地一致）⇒ 新逻辑已在生产生效。
- [x] 各位宿舍长在群里发 **`@威尼斯饺子 绑定 本人学号`**（第一轮 15:04~15:2x **10 位**，第二轮 18:15 补 **葛鹏展 `261880546`、陈奕瑾 `261880238`**，第三轮 19:40 补 **王天宇 `261880547`**，累计 **13 / 14 位**，仅剩陶三-1112 黄一渃 `261880334`）。13 条逐行核对全部通过：学号均命中「宿舍长学号」列、宿舍号与姓名与机器人回复文案完全一致、openid 无重复、格式均为 32 位十六进制。
- [x] 将收集结果写入云函数环境变量 `QQ_ROSTER_JSON`（15:29 写入 10 条 → 18:15 增至 12 条 → 19:40 增至 **13 条**；均走控制台「函数配置 → 编辑 → 环境变量 → 保存」，函数共 4 个变量，**每次改完其余 3 个变量值逐字节未变、日志投递仍关闭**）。本机干跑复验：`msg_seq=1` 文本名单 14 个未报宿舍不变，`msg_seq=2` markdown 生成 **13 个真 @ 标签** + 末尾「以下宿舍长未绑定，请自行留意：陶三-1112（黄一渃）」；函数 URL 复验 `{"replied":"rollcall","pending":14,"sent":[false,false]}`（配置修改后无回归）。**映射只存在函数环境变量里，不写进仓库。**
- [x] 真实群复验（14:18）：发「查未点名」确实多出**第二条 markdown 消息**（`以下宿舍长未绑定，请自行留意：…`），渲染正常 ⇒ 两条被动回复 + markdown 通道在真实群成立。
- [x] 可靠性补丁（14:46 部署，8499 字节）：`_send()` 失败时**丢弃缓存 token 后重试一次**；`_post_json()` 捕获 `HTTPError` 并**把 QQ 响应体里的 `code`/`err_code`/`message` 带进异常**，日志从此能看到失败原因。云端「测试」面板复验：假群号下 `msg_seq=1/2` 各失败两次、中间各刷新一次 token（`expires_in=4145/4144`），错误体 `11255/40011028 请求的资源不存在` 完整可见 ⇒ 重试与错误透出均生效（**34 项单测全绿**）。
- [ ] **14:37 群里发「绑定261880326」无回复的确切原因仍未定**（该学号不在宿舍长列，本应回复「点名表里没有学号…」）。原猜测「调试换票顶掉旧票」已被 14:52 实测否定，见 `docs/qq_bot_api.md` §1。**已定案（班长 15:05）：不开通 CLS 日志投递，靠自动重试兜底**——代价是真实推送的日志仍然看不到，同类问题只能靠监控耗时与复现推断；控制台「测试」面板只能看自己发起的调用。
- [x] 令牌临期自报（15:15 部署，9092 字节）：`api.token_hours_left()` 离线解 JWT 的 `exp`，**余量 ≤ 48 小时**才在「查未点名」回复末尾追加一行「【提醒】点名数据凭证将在约 N 小时后到期…」（余量充足时一个字都不加，免得刷屏干扰同学）；解析失败静默跳过，绝不让提醒把正常回复带崩。**40 项单测全绿**，云端复验 `{"replied":"rollcall","pending":14,"sent":[false,false]}` 无回归。当前令牌实测余量 55 小时（到期 2026-09-22 22:21），故 9/21、9/22 晚上的查询会自动带出提醒。
- [ ] 真实群终验：登记 `QQ_ROSTER_JSON` 后发「查未点名」，确认未报宿舍长被真 @ 并收到提醒。

### 阶段 5：上线审核与运维监控
- [ ] 提交审核、真机实测、额度告警配置。
- [ ] 令牌续期运维方案定稿：**「回复末尾自报余量」已于 2026-09-20 15:15 上线**（余量 ≤ 48 小时才提示）；自动换票已实测走不通（`table.nju.edu.cn` 走南大 CAS，账号密码换不到令牌，见 `docs/rollcall_api.md`），剩下的长期方案只有「请表格所有者签发永久 API Token」。

---

## 三、 当前进度追踪

> **当前所处阶段**：阶段 4 已完成验收（2026-09-20 13:28）→ **阶段 4.5：10 位宿舍长的 openid 映射已写入云函数环境变量 `QQ_ROSTER_JSON` 并生效（15:29），真 @ 已在本机干跑中生成，仅剩「真实群终验」一项 + 补齐 4 位未绑定宿舍长**  
> **关键进展**：云函数 `rollcall-bot` 已上线并跑通云端全链路（拉取真实点名数据 + 获取 QQ token，约 0.5~0.8 秒）；QQ 开放平台回调为函数 URL、**op=13 验签通过、接入方式 Webhook、接收事件「全部接收」已持久化**；**「真实群 @ 无回复」已定位为代码缺陷并修复上线**——`bot.py` 曾按 `type` 字段路由事件，而官方网关信封的事件类型字段是 `t`，故每次真实推送都在 1 毫秒内被当作未知事件忽略；修正路由 + 测试夹具对齐官方信封（20 项单测全绿）+ 文档纠错后重新打包部署，**并已由用户在本班群实测成功：`@威尼斯饺子 查未点名` → 机器人正常回复名单**。  
> **函数 URL**：`https://1491841992-bb3xh0y08p.ap-guangzhou.tencentscf.com`（公网访问 / 授权「开放」/ 参数兼容启用）  
> **已知运维事项**：点名接口访问令牌（JWT）有效期约 3 天，需定期更新环境变量 `ROLLCALL_API_TOKEN`（方法与备选方案见 `docs/rollcall_api.md`）；QQ access token 实测有效期约 50 分钟（代码按返回值动态缓存，无需干预）；**腾讯云控制台与 QQ 开放平台的网页登录态约 1 小时过期**，浏览器自动化操作前需用户重新扫码；账号主体「未认证」会锁住「服务范围」四个开关（影响公开使用与全班准入，进群上限），但**实测并不阻塞群事件推送**——群主所在群 @ 机器人时事件确实到达了函数 URL，故「机器人无回复」不能归因于认证；「开发体验号码」（上限 20，配置后不受服务范围影响）已加入管理员本人 QQ 作为额外保险；面向全班真实使用仍需个人主体认证。  
> **性能（2026-09-20 已优化）**：纯标准库 Ed25519 在 128MB 规格（仅约 0.1 核）上原为 2.9 秒，会触发 QQ 平台保存设置时的回调验签超时（`retcode 40001 签名校验不通过` / `40002 回调地址状态同步失败`）；改为「扩展坐标免求逆加法 + 预计算 `2^i·B` 表 + 公钥按种子缓存」后降至 **0.2 秒**，因此无需再调高内存规格。

---

## 四、 下一步行动指南 (Next Steps)

### 👨‍ 您（项目所有者）现在需要做的事：
1. **【登记已完成 13 / 14 位，请做终验 + 补齐最后 1 位】**
   - **终验（最重要）**：请在班群发一次 `@威尼斯饺子 查未点名`。现在应有两条消息：第一条是 14 个未报宿舍的文本名单（格式不变）；第二条是 markdown 消息，**已绑定的 13 位宿舍长会被真 @（蓝色、对方收到红点提醒）**，末尾以「宿舍号（姓名）」列出仍未绑定的那 1 位。请确认被 @ 的同学确实收到提醒。
   - **最后 1 位未绑定**，请让本人在群里发一次 `@威尼斯饺子 绑定 本人学号`，并把回复最后一行「学号=openid」复制给我：
     陶三-1112 黄一渃 `261880334`
   - 学号填错会得到「点名表里没有学号 xxx 对应的宿舍长」，让同学核对后重发即可；「绑定」和学号之间有没有空格都可以（14:28 修复）。
   - **别用您自己的学号 `261880326` 测**——它不在「宿舍长学号」列，只会得到「点名表里没有学号…」的提示。
   - **14:46 起**：群回复发送失败会自动换票重试一次，仍失败才放弃（此前是失败即静默）。
   - 临时测试指令 `测试at` 已从代码中删除，不必再发。
   - 以后再有同学新绑定或换人，把新的「学号=openid」发给我即可，我改一次环境变量就生效，不需要重新部署代码。
2. **账号主体认证（个人认证即可）**：本次不是它挡的，可以往后放。但如果要让全班同学正常使用、或提交上线审核，仍需完成认证（需实名手机号 + 身份证），认证后「群聊」服务范围才能开启、机器人进群上限 500。
3. （可选，一劳永逸）**联系晚点名表格所有者**（学院管理员）为 base 生成永久 API Token，替代每 3 天手动续期的 JWT，详见 `docs/rollcall_api.md`。

---

## 五、 变更记录
- **2026-09-20 20:20**：**新增对外《班级晚点名 QQ 机器人搭建教程》**（`docs/班级机器人搭建教程.html` + 同名 `.pdf`，13 页 A4，README 已加链接）。面向其他班零编程基础的班长，主线是「clone 本仓库 → 只改环境变量 → 上传 zip → 填回调」，含 8 步流程、5 列列名要求、F12 取 URL/page_id/令牌、函数 URL 勾选「参数兼容」、控制台测试面板看执行日志、`QQ_ROSTER_JSON` 绑定流程、每 3 天换令牌、7 行排查表、费用与合规提醒、三条硬规矩，附录另列指令一览、「改哪一行」定位与本仓踩过的 8 个坑。**发布前逐项核对代码，改掉四处失真**：① 删掉不存在的 `QQ_BOT_TOKEN` 环境变量（生产只有 `QQ_BOT_APP_ID`/`QQ_BOT_APP_SECRET`/`ROLLCALL_API_URL`/`ROLLCALL_PAGE_ID`/`ROLLCALL_API_TOKEN` 五项，`QQ_ROSTER_JSON` 为第六项可选）；② 删掉「改机器人名字要动 `MENTION_STRIP`」的错误指引（实际常量是 `MENTION = re.compile(r"<@!\d+>")`，按 `<@!数字>` 标记剥离 @，与机器人叫什么无关）；③ 自检 `fetch` 的请求头统一为实测可用的 `Authorization: Token <JWT>`；④「帮助」指令经 `bot.py:80` 确认真实存在。**隐私处理**：示例学号与 openid 全部换成明显假值（`261880001` / `1111…AAAA`），全文正则复查确认不含真实姓名、真实 openid、AppID、令牌；`.gitignore` 只排除 `.env`，故本教程会随下次提交进入公开仓库（已确认内容干净）。工具链记录：本机无 pandoc/wkhtmltopdf/reportlab，PDF 由 **Edge 无头打印**生成（`--headless=new --user-data-dir=<临时目录> --no-pdf-header-footer --print-to-pdf=<绝对路径>`，直接写 docs 目录会因既有 Edge 实例占用配置目录而报「拒绝访问」）；已校验输出为 A4、13 页、中文以 MicrosoftYaHei 子集内嵌（任何设备可读）。
- **2026-09-20 19:40**：**第三轮登记，`QQ_ROSTER_JSON` 增至 13 / 14**（新增南二-510 王天宇 `261880547`，核对同样零误配）。同一套控制台流程改环境变量并保存，其余 3 个变量值未变、日志投递仍关闭；本机干跑 `msg_seq=2` 现生成 **13 个真 @ 标签**、末尾仅剩陶三-1112（黄一渃），函数 URL 复验 `{"replied":"rollcall","pending":14,"sent":[false,false]}` 无回归。**「补绑定」已被证明是低成本例行操作**（同学发「绑定」→ 班长转「学号=openid」→ 改环境变量 → 干跑复验，约 2 分钟，不碰代码不重新部署），以后换宿舍长走同一条路。
- **2026-09-20 18:18**：**第二轮登记：`QQ_ROSTER_JSON` 由 10 条增至 12 条**（新增南二-509 葛鹏展、陶三-1110 陈奕瑾，核对同样零误配）。按 15:29 同一套流程改环境变量并保存，其余 3 个变量值逐字节未变、日志投递仍关闭；本机干跑 `msg_seq=2` 现生成 **12 个真 @ 标签**、末尾仅剩陶三-1112（黄一渃）与南二-510（王天宇），函数 URL 复验 `{"replied":"rollcall","pending":14,"sent":[false,false]}` 无回归。**另做提交前安全扫描**（班长自行 git 提交）：`.gitignore` 的 `.env` 规则已覆盖 `config/.env`、仓库仅跟踪 `.env.example`（空值模板）；全仓正则扫 JWT/`eyJ…`、App Secret、32 位 openid，**唯一命中是 `local_e2e.py:45-46` 自造的假夹具 `ABCD1234…`** ⇒ 真实凭据与同学们的个人标识都不会进仓库。
- **2026-09-20 15:32**：**10 位宿舍长的 openid 登记进 `QQ_ROSTER_JSON`，真 @ 链路在数据侧闭环**。① 班长把群里 10 条「绑定成功」回复原文转来，逐条核对结果全部干净：10 个学号均命中 SeaTable「宿舍长学号」列、宿舍号与姓名与机器人自己的回复文案逐字一致、openid 无重复且均为 32 位十六进制（**说明「绑定」的学号→身份核对逻辑在真实使用中没有出现误配**）；② 经控制台「函数配置 → 编辑 → 环境变量」保存，函数现有 4 个变量（`QQ_BOT_APP_ID` / `QQ_BOT_APP_SECRET` / `ROLLCALL_API_TOKEN` / `QQ_ROSTER_JSON`），**日志投递保持关闭**，映射只存环境变量、不入仓库；③ 本机干跑（模拟事件 + mock 发送）确认 `msg_seq=2` 的 markdown 已生成 **10 个 `<qqbot-at-user>`**、末尾以「宿舍号（姓名）」列出未绑定的 4 位（陶三-1110 陈奕瑾 `261880238`、陶三-1112 黄一渃 `261880334`、南二-509 葛鹏展 `261880546`、南二-510 王天宇 `261880547`），`msg_seq=1` 文本名单不变；④ 函数 URL 复验 `{"replied":"rollcall","pending":14,"sent":[false,false]}`（2.07 秒），配置修改未引发回归。**仅剩真实群终验**（需班长发一次「查未点名」确认对岸真被 @ 到、收到红点）。
- **2026-09-20 15:20**：**「令牌临期自报」上线（9092 字节，15:15 部署）**。① `api.token_hours_left()` 直接离线解 `ROLLCALL_API_TOKEN` 这串 JWT 的 `exp`（base64url 补 `=` 后解 payload），不需要发请求就能算出剩余小时数；② `token_expiry_notice()` **只在余量 ≤ 48 小时时**产出一行提醒，`build_rollcall_reply()` 的两条返回路径（正常 / 空表）末尾按需追加——**余量充足时一个字都不加**，避免每天给全班同学刷一行管理员才关心的话；③ 解析失败（非 JWT、`exp` 异常）一律静默返回 `None`，提醒功能绝不把主回复带崩；④ 新增 6 项单测，共 **40 项全绿**，`local_e2e.py` 真实数据下确认当前余量 55 小时故不显示提醒，云端函数 URL 复验 `{"replied":"rollcall","pending":14,"sent":[false,false]}` 无回归。**两项决定**：班长选择**不开通 CLS 日志投递**（继续靠 14:46 的自动重试兜底，代价是真实推送的失败原因仍不可见）；令牌提醒方式定为「回复自报余量」，不设 Qoder 定时提醒。**第三次佐证令牌是共享倒数**：14:52 测得 `expires_in=4145`，15:05 测得 `3509`，差值 636 秒与两次实测的墙钟间隔一致 ⇒ 全平台共用同一张票，换票不会互相顶掉，但也意味着**票的绝对到期时间不因我们重新获取而推迟**。
- **2026-09-20 14:55**：**「函数跑了、群里没回复」的可观测性补丁上线（8499 字节，14:46:30 部署）**。起因是 14:37 群里 `@机器人 绑定261880326` 无回复（监控 1 次调用 / 404ms）。① `bot._send()` 改为**失败即丢弃缓存 token 并重试一次**；② `qq_sender._post_json()` 捕获 `urllib.error.HTTPError` 并**读取响应体**，异常文本从毫无信息量的 `HTTP Error 400: Bad Request` 变成 `HTTP 400 /v2/groups/xxx/messages -> {"code":11255,"err_code":40011028,"message":"..."}`；③ 新增该路径单测，**34 项全绿**。④ **诊断通道突破**：SCF 控制台「函数代码 → 测试」面板自带**执行日志**（`START RequestId` + 全部 `print` + `Report Duration`），事件按 `{"body": "<QQ事件JSON字符串>", "isBase64Encoded": false}` 填即可，**不必开通 CLS**；据此复验：假群号下两条消息各失败两次、中间各刷新一次 token，重试与错误透出均按设计工作。⑤ **推翻自己先前的猜测**：约 1 秒内两次 `getAppAccessToken` 返回 `expires_in=4145/4144`（并非固定 7200），说明服务端给的是同一张票的剩余有效期在倒数 ⇒ 「我调试时换票把云函数缓存的旧票顶掉」**不成立**，14:37 的确切原因仍未定，要看真实推送的日志必须开 CLS（按量计费，待班长同意）。另：本机确认点名表 14 位宿舍长学号均为 9 位数字，`261880182` 在列、**班长本人学号 `261880326` 不在列**（故其「绑定」只应得到「点名表里没有学号…」的提示）。
- **2026-09-20 14:28**：**修复「绑定」必须带空格的可用性缺陷并重新部署（8049 字节）**。同学在群里发 `@威尼斯饺子 绑定261880182`（**学号前没有空格**）完全无回复——原因是 `bot.py` 的路由写成 `content == "绑定" or content.startswith("绑定 ")`，无空格串落到「无关消息」分支被静默忽略。改为 `content.startswith("绑定")`（参数仍 `strip()`，全角空格也吃），这样任何以「绑定」开头的消息都至少得到一条反馈而非沉默。新增回归测试（无空格必须走通绑定），**32 项全绿**；本机确认 `261880182` 确实在点名表「宿舍长学号」列中（该列全部为 9 位数字字符串）；函数 URL 复验：`绑定261880182` → `{"replied":"text"}`、`测试at` → `{"ignored":"测试at"}`（证明探针已下线、线上为新代码）。**待通知**：请那位同学重发一次（有无空格都可以）。教训：面向非技术用户的指令解析**不能要求精确空格**，且「无回复」的默认分支应尽量给出提示。
- **2026-09-20 14:18**：真实群复验通过第一步——用户发「查未点名」后机器人**多出第二条 markdown 消息**（当前无绑定，故内容为「以下宿舍长未绑定，请自行留意：…」），显示正常 ⇒ 「两条被动回复 + markdown 通道」在真实群成立，仅剩登记 `QQ_ROSTER_JSON` 后的真 @ 终验。用户已去组织宿舍长发「绑定」。
- **2026-09-20 14:11**：**「@ 未报宿舍长」（路线 B）实现并部署上线**。① `bot.py` 新增 `绑定 <学号>` 指令：取事件 `d.author.member_openid`，用 `api.find_leader()` 与 SeaTable「宿舍长学号」列核对，成功则回复「绑定成功 + 学号=openid」交由班长登记（**身份核对靠学号而非 QQ 号**，QQ 号方案已因 `author.id ≠ QQ号` 作废）；② 「查未点名」改为两条被动回复——`msg_seq=1` 原有文本名单不动，`msg_seq=2` 用 markdown（`msg_type:2`）把已绑定宿舍长渲染成蓝色 @，未绑定的仍列「宿舍号（姓名）」，一条都不会漏；③ `api.py` 抽出 `summarize()`（分类）与 `find_leader(rows, 学号)`（纯查表，不再自行发请求），`build_rollcall_reply()` 复用 `summarize()`；④ **删除一次性探针 `测试at`**；⑤ 单测重构为 **31 项全绿**——路由类测试改为 mock `api.fetch_rollcall_rows`（`bot.py` 现在直接拉数据，mock 上层会打真实接口），新增 @ 清单/roster 容错/绑定三分支用例；`local_e2e.py` 存根补 `msg_type` 参数并新增「绑定」两用例（含从真实表格首行取学号），本机验证四类回复文案均正确；⑥ 代码包 8062 字节经控制台「本地上传zip包」部署（注意「提交方法」下拉默认停在上一项，需先选「本地上传zip包」再传），函数 URL 复验 `{"replied":"rollcall","pending":14,"sent":[false,false]}`，`pending=14` 与本机一致，证明云端为新代码。**待办**：宿舍长发「绑定」→ 班长收集「学号=openid」→ 写入环境变量 `QQ_ROSTER_JSON` → 群里终验真 @。
- **2026-09-20 14:02**：**@ 能力实测突破——`<qqbot-at-user>` 只在 markdown（`msg_type:2`）里渲染成蓝色 @，纯文本（`msg_type:0`）里原样显示成代码**（13:55 条目里「暂未跑通」的判断是只测了纯文本所致，已据实改写 `docs/qq_bot_api.md` §6）。⇒ 「@ 未报宿舍长」**技术上可行**，回复需改用 markdown 消息。~~**剩余唯一待验门槛**：能否 @ 「非触发者」——前两次测试目标都是发消息的人本人，与平台自动 @ 混淆；需同学 A 先发 `测试at`（汇总消息里会打印其 openid），群主再发 `测试at <A的openid>` 复验。~~（**14:11 前已复验通过：能 @ 非触发者**，见 §6 实测三）**存储方案（零新增产品）**：不引入 COS、不改班级共享表格，改为「绑定结果由群主收集 → 以函数环境变量 `ROSTER` 注入」，宿舍名单一学期基本固定，人工维护成本可接受；身份核对用「绑定消息内附宿舍长学号 + 与 SeaTable 学号列比对」替代（QQ 号方案已随 `author.id ≠ QQ号` 的实测结果作废）。
- **2026-09-20 13:55**：`测试at` 探针真实群首测结果（详见 `docs/qq_bot_api.md` §6，已据实改写）：① 事件 `d.author` **不含 `user_openid`**，且 `id` 与 `member_openid` 是同一个 32 位十六进制串、**不是 QQ 号** ⇒ 项目所有者手上的「宿舍长 QQ 号名单」无法用于自动核对身份，官方亦无「QQ号→openid」转换接口；② 机器人**每条被动回复都被平台自动冠上 `@触发者`**，故「好像都 at 到我」不能作为我方 @ 生效的证据；③ `<qqbot-at-user id="" />` **接口返回成功但客户端原样显示成代码、未渲染成 @** ⇒ 当前（个人主体未认证 + 群聊被动回复）出站 @ **暂未跑通**。**已部署升级版探针**（一次发三种写法：文本+新标签 / 文本+旧协议 `<@!id>` / **markdown `msg_type:2`**+新标签，并新增 `测试at <openid>` 参数以 @ 「非触发者」来排除自动 @ 干扰；`qq_sender.send_group_reply` 增加 `msg_type` 支持），新增 markdown 载荷格式单测，共 **24 项全绿**，代码包 7033 字节已部署并经函数 URL 确认生效（假群号下三种写法均返回 400，符合预期）。待所有者二次实测；若三种写法都不渲染，则「@ 未报宿舍长」在认证/权限变化前不可实现，改走「文本点名 + 定时主动播报」替代方案。
- **2026-09-20 13:45**：项目所有者选定「@ 未报宿舍长」走 **路线 B（绑定一次后真 @）**。动手前先补一次取证，避免重蹈「照假想写代码」的覆辙：新增一次性探针指令 `测试at`（`src/bot.py::_at_probe`，把结论发回群里，因为函数返回体我们看不到），用于一次验清三件事——① 平台是否放行 `<qqbot-at-user id="" />`（未认证主体 + 被动回复场景）；② `id` 该填 `user_openid` 还是 `member_openid`；③ 事件 `author.id` 是否就是 QQ 号（若是，所有者手上的「宿舍长 QQ 号名单」可直接用于自动核对绑定身份，无需白名单接口）。新增 3 项单测（共 **23 项全绿**），代码包 6889 字节已部署，函数 URL 用假群号自测返回 `{"at_probe": {...400...}}`，证明新路由已生效（旧代码会返回 `ignored`）。待所有者在真实群发 `@威尼斯饺子 测试at` 反馈。
- **2026-09-20 13:30**：**阶段 4 验收通过**——用户在本班群发送 `@威尼斯饺子 查未点名`，机器人按预期回复未报名单，全链路（平台推送 → 函数路由 → 点名查询 → 群回复）真实环境跑通，13:15 的路由修复经实测确认有效。随后调研用户新提出的「直接 @ 未报宿舍长」需求，结论已写入 `docs/qq_bot_api.md` §6：出站 @ 语法官方为 `<qqbot-at-user id="" />`（嵌于文本/markdown，旧协议 `<@userid>` 已废弃），**可行性瓶颈不在发送而在「人 → openid」映射**——群成员列表接口 `/v2/groups/{group_openid}/members` 官方标注「仅白名单机器人可用，需联系平台运营申请权限」；备选是被动收集（`d.author` 带 `user_openid`/`member_openid`/`username`，但需外接持久存储）。已向项目所有者提出 A（昵称文本点名）/ B（绑定指令 + 存储）/ C（申请白名单）三条路线待其选择。
- **2026-09-20 13:15**：**「真实群 @ 机器人无回复」根因定案并修复上线**。真凶是**一个字段名**：官方《通用数据结构》（`bot.q.qq.com/wiki/develop/api-v2/dev-prepare/event-emit/payload.html`）定义事件信封为 `{"id","op":0,"d","s","t"}`，**事件类型放在 `t`，官方结构里根本没有 `type`**；而 `src/bot.py` 按 `body["type"] == "GROUP_AT_MESSAGE_CREATE"` 路由，因此每次真实推送都在 1 毫秒内落到 `ignored` 分支静默丢弃。取证链：SCF 分钟级监控显示 09:21:00 恰有 1 次调用（证明平台确实推到了函数 URL）→ 该次 Duration 仅 1ms（对照组自测全链路 436ms、op=13 约 200ms，说明未进入任何业务分支）→ 直读官方 payload 文档定案。**同时推翻 09:34 的初判**（「未认证主体 → 服务范围锁死 → 平台不推送群事件」）——认证状态影响的是公开使用与全班准入，不是这次事件推送的阻塞。**四处修复**：① `bot.py` 改按 `t` 路由；② 单测夹具与 `local_e2e.py` 改为复刻官方五字段信封（此前正是照自作假想拼数据才让同一个错误被验证两遍），新增 2 项回归测试（其他 `t` 事件应忽略、`type` 字段不得被路由），**20 项单测全绿**；③ `docs/qq_bot_api.md` §4 订正事件信封格式并写入事故记录；④ 重新打包 6294 字节经控制台「本地上传zip包」部署，函数 URL 复验：`t` 事件正确进入消息处理（假群号故返回 `send_failed`，0.51~0.56 秒）、旧 `type` 事件返回 `ignored:"0"`（证明线上为新代码）、op=13 验签 0.16 秒。**剩余**：用户在班级群 @「查未点名」复验。
- **2026-09-20 09:34**：定位「群里 @ 机器人无回复（09:21 那次）」根因：~~**非代码/部署问题**~~（**该结论已于 13:15 被推翻，真凶是 `bot.py` 按 `type` 而非官方 `t` 路由，见上一条**）。当时依据 QQ 后台「累计进群 0」、运营数据群/消息表全为 0 项，对照官方《QQ Bot介绍与接入指南》5.1 与实测四个服务范围开关 `is-disabled`，判定主体未认证导致平台不推送群事件——实际监控后来证明事件已推达函数 URL，故该判定不成立，仅说明「未认证会锁服务范围」而非锁事件推送。**已落地**：按官方 8.3「开发体验号码——配置后不受服务范围设置影响」，把超级管理员 QQ `2366759931` 加入机器人「开发体验号码设置」，保存后 1/20 且刷新页面仍在（作为额外保险保留）。排障过程中曾对搜索结果里的一个 OSS 代理链接做了 3 次抓取、返回内容互相矛盾，已判定不可信并弃用，结论仅采用官方站点直读文本。
- **2026-09-20 09:20**：阶段 4 配置全部完成。**根因定位**：「接收事件配置」保存后静默回退并非点击未生效，而是接口返回 `retcode 40001 签名校验不通过` / `40002 回调地址状态同步失败`——QQ 平台保存设置时会重新向回调地址发 op=13 验签，而云端签名耗时 2.9~3.1 秒正撞 3 秒超时。**修复**：重写 `src/ed25519.py`（扩展坐标 (X,Y,Z,T) 完全加法定律、单次加法不再 `pow(...,q-2,q)` 求逆；预计算 `2^i·B` 表使按量乘变为纯加法；公钥与私标量按种子缓存），18 项单测含 RFC 8032 向量全绿、签名结果与优化前逐字节一致，重新打包部署后云端 op=13 由 2.9 秒降至 **0.17~0.22 秒**，群消息全链路 0.51~0.83 秒。随后「全部接收」保存返回 `retcode:0`，查询回读 `all_subscription:true`，刷新后仍保持。另记：QQ 开放平台后台直达链接 `.../manage/<appid>?sec=...&sub=event-callback` 在登录态新起时会跳回 `q.qq.com/#/`，需从「我的机器人」点入卡片；账号主体「未认证」导致服务范围四个开关 `is-disabled` 不可改；机器人「累计进群 0」，真实群实测需用户先把它邀请进群。
- **2026-09-20 08:45**：修复版代码包已上传部署（控制台部署日志 08:41:13，函数代码区可见 `bot.py`/`api.py`/`qq_sender.py`/`ed25519.py` 四文件）。经函数 URL 云端复验三项：① 群消息事件返回 `HTTP 200 {"send_failed": "HTTP Error 400: Bad Request"}`，证明异常捕获修复已在生产生效（修复前同场景为 430 崩溃）且点名查询与 token 获取全通，单次 690ms；② op=13 事件正常返回 `plain_token` + 128 位十六进制签名，但稳定耗时约 2.9 秒（本机 <0.1 秒），定位为纯标准库 Ed25519 属 CPU 密集而 128MB 规格仅约 0.1 核，已记为可选优化项（提内存至 512MB）；③ 确认参数兼容模式下事件体即 QQ 原始 JSON（无需外层 `body` 包装），且 ~~`type: "GROUP_AT_MESSAGE_CREATE"` 为路由必需字段~~（**此判断错误，正是本 bug 的来源**，官方信封的事件类型字段是 `t`，见 13:15 条目）。收尾受阻：QQ 开放平台网页登录态再次过期（访问后台被重定向至 `q.qq.com/#/` 登录页），待用户扫码后保存「全部接收」。
- **2026-09-20 08:23**：阶段 4 主体完成——腾讯云 SCF 创建事件函数 `rollcall-bot`（广州/Python 3.9/`bot.main_handler`/128MB/10s/公网访问）并注入三项环境变量；因 API 网关触发器已对新用户下线，改用**函数 URL**（公网 + 授权「开放」+ 参数兼容），实测该模式下集成响应结构可被正确解包，`bot.py` 无需改造；云端出网双链路实测通过（`table.nju.edu.cn` 点名数据拉取成功、`api.bot.qq.com` token 获取成功 `expires_in=881`，全链路 691ms）；QQ 开放平台回调地址保存成功，**op=13 Ed25519 签名校验通过**，接入方式由 WebSocket 切换为 Webhook。修复 `bot.py` 一处生产缺陷：`send_group_reply` 异常会冒泡致云函数 430 崩溃、平台重推消息，现已捕获并返回 200 + `send_failed`（本地验证通过，新代码包待上传）。中断原因：腾讯云控制台与 QQ 开放平台网页登录态均过期，待用户重新扫码后继续收尾三项。
- **2026-09-19 23:40**：阶段 3 完成——用户提供 QQ 凭据并写入 `config/.env`；实测凭据有效（access token 获取成功，实测有效期约 50 分钟而非文档的 7200 秒，代码动态缓存不受影响）；新增 `local_e2e.py` 本地全链路联调脚本（op=13 回调、群消息真实数据、base64 请求体三用例全过，单次 0.04~0.10 秒）；伪造群号诊断证实发送接口鉴权与格式正确（仅报群不存在 code 11255）；更新 `docs/qq_bot_api.md` 实测记录。下一步：阶段 4 云端部署（等用户注册腾讯云）。
- **2026-09-19 23:10**：阶段 2 完成——实测破解点名数据源（南大表格 SeaTable 外部应用：`Authorization: Token <JWT>` 请求头、行数据按 metadata key 存放、19:00 自动化规则清空当日字段）；新增 `src/api.py`（动态列解析 + 未报/异常/已报汇总）、`docs/rollcall_api.md`（接口与令牌续期文档）、`config/.env`（本地真实令牌，gitignore 已排除）；`bot.py` 接入真实查询并增加失败兜底；单测增至 17 项全绿，本机端到端冒烟测试通过。
- **2026-09-19 22:20**：阶段 2 代码骨架落地——`src/bot.py`（事件入口、Ed25519 回调验证、指令路由）、`src/qq_sender.py`（access token + 群回复）、`src/ed25519.py`（纯标准库 Ed25519 参考实现）、`tests/`（9 项单测全绿）、`docs/qq_bot_api.md`（官方 API 备忘）、`config/.env.example`、`.gitignore`、`deploy/README.md`。剩余唯一阻塞：点名系统真实 JSON 结构（见上方行动指南）。